# x-ui服务器地址
XUI_SERVER_URL=http://localhost:54321

# x-ui的API Key（如果为空，服务启动时会自动创建）
XUI_API_KEY=

# x-ui的API Secret（如果为空，服务启动时会自动创建）
XUI_API_SECRET=

# x-ui数据库路径（用于自动创建API Key）
XUI_DB_PATH=/etc/x-ui/x-ui.db

# 后端代理服务端口
PROXY_PORT=8080

# 默认协议（vmess/vless/trojan/shadowsocks）
DEFAULT_PROTOCOL=shadowsocks

